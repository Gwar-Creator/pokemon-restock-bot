name: Proshop Discovery Probe

on:
  workflow_dispatch:
  push:
    paths:
      - "proshop_discovery_probe.py"
      - ".github/workflows/proshop_probe.yml"
      - "restock_bot_github.py"

permissions:
  contents: read

concurrency:
  group: proshop-discovery-probe
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Validate probe
        run: python -m py_compile proshop_discovery_probe.py

      - name: Run Proshop discovery probe
        env:
          DISCORD_WEBHOOK_URL: "https://example.invalid/not-used"
        run: python proshop_discovery_probe.py
